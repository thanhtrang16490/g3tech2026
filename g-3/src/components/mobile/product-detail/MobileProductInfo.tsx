import React from 'react';
import type { Product } from '../../../types';

interface MobileProductInfoProps {
  product: Product;
}

export default function MobileProductInfo({ product }: MobileProductInfoProps) {
  // Component removed - no content to display
  return null;
}